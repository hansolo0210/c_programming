#include <stdio.h>
#include <stdlib.h>

int main()
{
char colors[20];
printf("Enter a color: ");
scanf("%s",colors);
printf("The color you entered is %s",colors);
int numbers[10];
numbers[2]=80;
printf("\n the number is %d",numbers[0]);
}
